apiVersion: v1
kind: Service
metadata:
  namespace: biodiversity
  name: wetlands-v2
  labels:
    app: wetlands-v2
spec:
  type: ClusterIP
  selector:
    app: wetlands-v2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
